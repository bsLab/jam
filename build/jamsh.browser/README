JAM Interpreter Shell for WEB Browser
---------------------

> load('class.js')
> out(['SENSOR',1])
> t=in(['SENSOR',_])
> a=run(explorer,{dir:DIR.NORTH})
> kill(a)
> send(a,'MEAN','X')
> answer('neighbours','?')

